# FastAPI Backend Requirements

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0

# Pydantic for data validation
pydantic>=2.0.0

# YAML parsing (already in root requirements, but included for completeness)
pyyaml

# LLM providers (check root requirements for exact versions)
openai
anthropic

# For development
pytest>=7.0.0
httpx>=0.24.0  # For testing async endpoints
